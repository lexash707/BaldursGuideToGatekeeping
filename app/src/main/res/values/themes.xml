<!-- res/values/themes.xml -->
<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.BaldursGuideToGatekeeping" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary branding color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/dark_green</item>
        <item name="colorOnPrimary">@color/green</item>
        <!-- Secondary branding color. -->
        <item name="colorSecondary">@color/green</item>
        <item name="colorSecondaryVariant">@color/dark_green</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Button color. -->
        <item name="colorButtonNormal">@color/green</item>
        <item name="colorAccent">@color/green</item>
        <!-- Text color. -->
        <item name="android:textColor">@color/white</item>
        <!-- Window background color. -->
        <item name="android:windowBackground">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <!-- Navigation bar color. -->
        <item name="android:navigationBarColor">@color/black</item>
        <!-- Custom font. -->
        <item name="android:fontFamily">@font/playfair_display</item>
    </style>

    <style name="Theme.BaldursGuideToGatekeeping" parent="Base.Theme.BaldursGuideToGatekeeping">
        <!-- Customize your theme here if needed. -->
    </style>
</resources>
